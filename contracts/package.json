{
  "name": "vivero-control-contracts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node validate.mjs",
    "test": "node --test tests/contracts.test.mjs",
    "check": "npm run validate && npm test"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  }
}
